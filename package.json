{
  "private": true,
  "name": "@nfreear/notes-web-app",
  "scripts": {
    "start": "cd docs && npx servor . 8080 --reload",
    "fix": "npx semistandard --fix",
    "test": "npx semistandard"
  },
  "devDependencies": {
    "semistandard": "^16.0.1",
    "servor": "^4.0.2"
  }
}
